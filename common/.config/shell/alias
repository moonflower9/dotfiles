alias c="clear"
alias v="nvim"
alias ls="ls -la --color=auto --group-directories-first"
alias grep="grep --color=auto"
alias shell="exec $SHELL -l"

fk() { sudo fc -s; }

wal() { command wal "$@" -o "$HOME/.config/wal/post-hook.sh"; }
