# ZDOTDIR is exported in /etc/zsh/zshenv so zsh loads config from here.
# On a fresh system: echo 'export ZDOTDIR="$HOME/.config/zsh"' | sudo tee -a /etc/zsh/zshenv
#
# default programs
export EDITOR="nvim"
export BROWSER="firefox"
